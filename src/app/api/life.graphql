fragment LifeData on Life {
    firstName
    lastName
    fullName
    title
    description
    birthday
    hobbies
}

query getLifeByID($id: ObjectID!) {
    life: getLife(id: $id) {
        ...LifeData
    }
}

query getListLives {
    life: listLives {
        ...LifeData
    }
}